[tool.pytest.ini_options]
testpaths = ["src/tests"]
pythonpath = ["src"]
filterwarnings = [
    "ignore::UserWarning",
]

[project]
name = "alphaBT"
requires-python = ">=3.10"
dependencies = [
    "pydantic>=2.0.0",
]
